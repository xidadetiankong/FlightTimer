<!--pages/summary/summary.wxml-->

<!-- predresttime: 0, //下一个48的开始时间
    DATA: [],
    tenhourrest: true,//上一个任务结束到现在是否满足10小时
    foureighthourrest: true,
    nextciclestart: '',  -->
<view class="max">

	<image class="background" src="../../img/summary.jpg"></image>
	<view class="container">

		<view class="text1">
			<view class="statas">
				<view>您的总工作时间为{{totaldutytime}},包含{{totalFlightlegs}}个航段、{{totalLandings}}个落地。</view>

			</view>
			<view class="statas">
				<view>{{selectyear}}年您工作了{{YEARtotaldutytime}}小时,包含{{YEARtotalFlightlegs}}个航段、{{YEARtotalLandings}}个落地。</view>

			</view>




			<view class="tenrest">
				<view> 距离上次工作结束到现在您休息了{{currentREST}}。</view>
				<view>根据公司要求，航后要休息10+</view><input type="number" style="width:40rpx" maxlength="3" value="{{correction10REST}}" bindinput="restcorrec"></input>小时
				<view>（此处自行填写）</view>
				<view wx:if="{{tenhourrest}}">满足航后休息要求。</view>
				<view wx:else>,目前还不满足航后休息要求，请合理安排工作。</view>
			</view>

			<view class="policy">
<block>
<block wx:if="{{profession==='pilot'?true:false}}">
					<view>最近七个工作日您的工作时间为{{sevenDaywork}}</view>
					<view wx:if="{{sevenDayworkOvr}}">没有超过60小时的限制！</view>
					<view wx:else>超过了60小时的限制</view>
				</block>

				<block wx:elif="{{profession==='attendant'?true:false}}">
					<view>最近七个工作日您的工作时间为{{ATTsevenDaywork}}</view>
					<view wx:if="{{ATTsevenDayworkOvr}}">没有超过70小时的限制！</view>
					<view wx:else>超过了60小时的限制</view>
				</block>

</block>
				

				<block>
					<view wx:if="{{foureighthourrest}}">当前周期满足48小时休息期，您可以继续工作到{{predresttime}}。工作重要身体更重要记得锻炼身体哟！</view>
					<view wx:else>当前周期不满足48小时休息期，请合理安排工作<view>下一个工作周期开始时间为{{nextciclestart}}</view>
					</view>
				</block>


			</view>
		</view>



		<view class="showDetail" hidden="{{showDetail}}">
			<view class="detailData">
				<view class="title">
					<view>月份</view>


					<block>
						<block wx:if="{{profession==='pilot'?true:false}}">
							<view>值勤时间</view>
							<view>段数</view>
							<view>起落数</view>
						</block>
						<block wx:elif="{{profession==='attendant'?true:false}}">
							<view>值勤时间</view>
							<view>段数</view>
						</block>
						<block wx:elif="{{profession==='security'?true:false}}">
							<view>值勤时间</view>
							<view>段数</view>
						</block>
						<block wx:elif="{{profession==='others'?true:false}}">
							<view>工作时间</view>
						</block>
					</block>

				</view>
				<view class="historyData">
					<view class="historyKid">
						<view wx:for="{{twelveMonth}}" wx:key="index">
							{{item}}
						</view>
					</view>


					<block>
						<block wx:if="{{profession==='pilot'?true:false}}">
								<view class="historyKid">
						<view wx:for="{{totaldutytimeof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
					<view class="historyKid">
						<view wx:for="{{totalFlightlegsof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
					<view class="historyKid">
						<view wx:for="{{totalLandings12}}" wx:key="index">
							{{item}}
						</view>
					</view>
						</block>
						<block wx:elif="{{profession==='attendant'?true:false}}">
								<view class="historyKid">
						<view wx:for="{{totaldutytimeof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
					<view class="historyKid">
						<view wx:for="{{totalFlightlegsof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
					
						</block>
						<block wx:elif="{{profession==='security'?true:false}}">
							<view class="historyKid">
						<view wx:for="{{totaldutytimeof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
					<view class="historyKid">
						<view wx:for="{{totalFlightlegsof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
						</block>
						<block wx:elif="{{profession==='others'?true:false}}">
						<view class="historyKid">
						<view wx:for="{{totaldutytimeof12}}" wx:key="index">
							{{item}}
						</view>
					</view>
					
						</block>
					</block>

				



				</view>

			</view>
<button class="b1"  bindtap="returnTT" size="mini">返回</button>
		</view>


	<button class="b1" id="btnDDD" bindtap="showDetai" >点击查看详细</button>

		<view class="BTNgp">
			<button class="b1" bindtap="lastyear" size="mini">上一年</button>
			<view>{{selectyear}}年</view>
			<button class="b1" bindtap="nextyear" size="mini">下一年</button>
		</view>


		<view class="warning">以上数据仅供参考，请以公司规定或相关法律法规为准</view>

	</view>
</view>